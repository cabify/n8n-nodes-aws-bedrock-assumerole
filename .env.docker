# Docker Environment Variables for n8n Testing
# Copy this file to .env and fill in your AWS credentials

# AWS Credentials for testing
# These should be your base AWS credentials that can assume the target role
AWS_ACCESS_KEY_ID=your-base-access-key-id
AWS_SECRET_ACCESS_KEY=your-base-secret-access-key
AWS_REGION=us-east-1

# Optional: Role ARN to assume (can also be set in n8n credential UI)
AWS_ROLE_ARN=arn:aws:iam::123456789012:role/your-bedrock-role

# n8n Configuration
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=admin
