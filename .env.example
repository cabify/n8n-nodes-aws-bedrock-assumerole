# AWS Base Credentials (for AssumeRole)
# These are the credentials that will assume the target role
AWS_ACCESS_KEY_ID=your-base-access-key-id
AWS_SECRET_ACCESS_KEY=your-base-secret-access-key

# AWS Region
AWS_REGION=us-east-1

# Optional: Role ARN to assume (can also be set in n8n credential)
# AWS_ROLE_ARN=arn:aws:iam::123456789012:role/n8n-bedrock-role

# Optional: Session duration in seconds (default: 3600)
# AWS_SESSION_DURATION=3600
